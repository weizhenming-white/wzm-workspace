// test.cpp -- test

#include <iostream>
#include <cmath>

int main()
{
    using namespace std;

    // double m;
    // cin >> m;
    // double num = sqrt(m);
    // cout << num;

     double area;
    //  cout << "Enter the floor area, in square feet, of your home: ";
     cin >> area;
     double side;
     side = sqrt(side);
     cout << area << endl << side << endl;

    return 0;
}